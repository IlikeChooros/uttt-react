import type { NextConfig } from 'next';

const nextConfig: NextConfig = {
	/* config options here */
	// crossOrigin: 'use-credentials',
	// allowedDevOrigins: ['http://localhost:8080', 'http://192.168.0.103:8080'],
	// output: 'export',
	// images: {
	// unoptimized: true,
	// },
};

export default nextConfig;
